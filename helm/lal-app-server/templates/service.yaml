apiVersion: v1
kind: Service
metadata:
  name: {{ include "lal-app-server.fullname" . }}
  labels:
    {{- include "lal-app-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lal-app-server.selectorLabels" . | nindent 4 }}
