apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lal-app-server.fullname" . }}-test-connection"
  labels:
    {{- include "lal-app-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lal-app-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
